# AGENTS.md

## 프로젝트 개요

- 이 프로젝트는 React + TypeScript + Vite 기반의 프론트엔드 애플리케이션입니다.
- 주요 목적은 창업/상권/금융/사업성 시뮬레이션 관련 정보를 사용자에게 직관적으로 제공하는 서비스 UI를 구현하는 것입니다.
- 현재 프로젝트는 KB 브랜드 톤을 반영한 따뜻하고 부드러운 사용자 경험을 지향합니다.

## 기술 스택

- React 19
- TypeScript
- Vite 8
- React Router DOM
- Tailwind CSS 4
- Zustand
- TanStack React Query
- Axios
- clsx / tailwind-merge
- lucide-react

### 라이브러리 사용 원칙

- 이미 설치된 라이브러리를 우선 활용합니다.
- 새로운 라이브러리를 설치하기 전에 반드시 다음을 설명해야 합니다.
  1. 왜 필요한지
  2. 현재 설치된 라이브러리로도 해결 가능한지
  3. 프로젝트에 어떤 이점이 있는지
  4. 유지보수 및 번들 크기 영향
- 새로운 의존성 추가가 불가피할 때만 설치하고, 가능하면 기존 도구로 해결합니다.

## 작업 원칙

- 변경 전에는 현재 구조와 기존 패턴을 먼저 확인합니다.
- 기존 UI/레이아웃을 크게 깨뜨리지 않도록 최소 변경으로 작업합니다.
- 기능 추가, 리팩터링, 스타일 변경은 목적과 이유를 명확히 하고 진행합니다.
- 코드 수정을 시작하기 전에 반드시 이 파일(AGENTS.md)을 먼저 읽고, 작업 중 발생한 변경사항이 이 파일의 규칙과 충돌하는지 확인합니다.
- 코드 수정이 완료된 후, 이 파일에 반영이 필요한 규칙·원칙·작업 방식 변화가 있다면 함께 수정합니다.
- 작업 후에는 반드시 빌드 또는 관련 검증을 실행합니다.
- 사용자 요청이 명확하지 않으면 가정하지 말고 확인합니다.
- 코드 수정 범위는 요청된 작업과 직접 관련된 파일로 제한합니다.

## 폴더 구조

- src/
  - components/: 재사용 가능한 UI 컴포넌트
  - layouts/: 페이지 공통 레이아웃
  - pages/: 라우팅되는 화면 단위 페이지
  - lib/: 공통 유틸리티 및 API 헬퍼
  - store/: Zustand 상태 관리
  - providers/: 앱 전역 Provider
  - styles/: 전역 스타일 및 테마 설정
  - assets/: 이미지/아이콘 등 정적 자원

## 컴포넌트 원칙

- UI는 재사용 가능한 컴포넌트 단위로 구성합니다.
- 페이지 전용 로직은 가능한 페이지 내부에 두고, 공통 로직은 컴포넌트 또는 lib로 분리합니다.
- props는 명확하게 타입을 지정합니다.
- 중복되는 UI는 공통 컴포넌트로 추출합니다.
- 컴포넌트는 가능한 작고 명확한 책임을 가지도록 구성합니다.

## 레이아웃 원칙

- 공통 레이아웃은 layouts/에 두고, 페이지는 해당 레이아웃을 사용하도록 구성합니다.
- 페이지 간 일관된 여백, 카드 스타일, 타이포그래피를 유지합니다.
- 모바일/데스크탑 대응을 기본으로 하며, 반응형 레이아웃을 우선 고려합니다.
- 레이아웃 변경은 전역 구조와 충돌하지 않도록 주의합니다.

## AI 캐릭터 원칙

- 프로젝트의 AI 캐릭터는 친근하고 신뢰감 있는 톤을 유지합니다.
- 캐릭터 UI는 단순함과 부드러움, 안내형 메시지 전달에 집중합니다.
- 과도한 장식이나 과한 애니메이션보다 명확한 정보 전달을 우선합니다.
- 캐릭터 표현은 사용자 경험을 보조하는 역할로 사용합니다.

## 스타일 원칙

- Tailwind 클래스 기반 스타일을 우선 사용합니다.
- 디자인 시스템 색상은 하드코딩 대신 테마 토큰을 사용합니다.
- 가능하면 반복되는 색상/간격/라운딩 값을 일관된 클래스 조합으로 관리합니다.
- 기존 스타일과 충돌하지 않도록 전역 CSS와 테마 설정을 우선 확인합니다.
- 임시로 색상 값을 직접 넣는 방식은 지양합니다.

## 브랜드 컬러

- 메인 컬러
  - kb-yellow-positive: #FFA800
  - kb-yellow-negative: #FFCC00
- 텍스트/테두리
  - kb-gray: #60584C
  - kb-dark-gray: #60584C
- 서브 컬러
  - kb-background: #F5F2ED
  - kb-mid-tone: #8B7F73
  - kb-light-tone: #C4B9AD
  - kb-light-tone-2: #A59D91
- 배경/표면
  - kb-background-soft: #FAF8F5
  - kb-surface-secondary: #E8E2D6
  - kb-border-strong: #D4CFC8
  - kb-white: #FFFFFF

### 브랜드 색상 사용 방식

- 텍스트: `text-kb-gray`, `text-kb-mid-tone` 등 사용
- 배경: `bg-kb-background`, `bg-kb-yellow-positive` 등 사용
- 테두리: `border-kb-surface-secondary`, `border-kb-border-strong` 등 사용
- 기존 `text-[#60584C]` 같은 하드코딩 색상은 가능하면 토큰 기반으로 변경합니다.

## 작업 완료 기준

- 요청된 작업이 반영되어 있습니다.
- 관련 파일의 구조와 스타일이 기존 패턴과 충돌하지 않습니다.
- 빌드 또는 검증이 가능한 경우, 실제로 실행해 확인합니다.
- 변경 사항이 있다면 핵심 내용을 요약할 수 있습니다.
- 불확실한 부분은 사용자에게 확인을 요청합니다.

## 추가 원칙

- 가능하면 기존 파일을 덜 수정하는 방향으로 작업합니다.
- UI 변경은 화면 전체를 크게 바꾸기보다 현재 구조를 유지하면서 조정합니다.
- 코드 품질과 사용자 경험을 함께 고려합니다.
- 작업 중 발견한 개선점이 있더라도 본 요청 범위를 벗어나면 별도 논의합니다.
